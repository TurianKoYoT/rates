<% content_for(:title) { t("admin.title") } %>

<div class="container-fluid">
  <div class="alert alert-primary">Формат даты: год-месяц-день час:минута:секунда</div>
  <div class="row text-center">
    <% @rates.each do |rate| %>
      <div class="col-md-4 col-lg-3">
        <%= render 'forced_rate_widget', rate: rate %>
      </div>
    <% end %>
  </div>
</div>
